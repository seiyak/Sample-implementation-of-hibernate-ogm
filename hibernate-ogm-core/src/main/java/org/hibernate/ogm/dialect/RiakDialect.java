package org.hibernate.ogm.dialect;

import org.hibernate.dialect.Dialect;
import org.hibernate.ogm.dialect.mapreduce.MapReducable;

public class RiakDialect extends Dialect implements MapReducable {

}
